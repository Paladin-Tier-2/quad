# quad-solver

Tiny command-line quadratic equation solver in C (real + complex roots).

## Build

```bash
gcc quad.c -o quad -lm
```

## Usage

```bash
./quad a b c
```

Example:

```bash
./quad 1 -7 10
```
